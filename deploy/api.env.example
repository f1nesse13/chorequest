# Container env for API service (copy to api.env)
PORT=8080
AWS_REGION=us-east-1
DYNAMODB_ENDPOINT=http://dynamodb:8000
DYNAMO_AUTO_MIGRATE=1
DYNAMO_TABLE_NAME=chorequest
# Provide a non-empty secret for dev
JWT_SECRET=changeme
ENABLE_GRAPHIQL=1
