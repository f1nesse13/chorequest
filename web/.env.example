# Web environment (used by Node-side tooling like codegen)

# Where codegen fetches GraphQL schema from
GRAPHQL_SCHEMA_URL=http://localhost:8080/query

# Runtime GraphQL URL for the app; in dev, Vite proxy handles /query
VITE_GRAPHQL_URL=/query
